<div class="container">

  <h3 class="mt-3">Available Items</h3>
  <hr class="my-4">

  <div class="row mb-2">

    {{#each availItems as |item|}}


    <div class="col-md-3 mt-2">
      <div class="card text-center">
        <div class="card-header  bg-dark text-light">
          {{item.name}}
        </div>
        <div class="card img-fluid">
          <img class="card-img-top" src="{{item.img_url}}" alt="Card image" style="width:100%">
          <div class="card-img-overlay">
            {{!-- <h5 class="card-title">Special title treatment</h5> --}}
            {{!-- <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
            <a href="#" class="btn btn-primary">Go somewhere</a> --}}
          </div>
        </div>
        <div class="card-footer text-muted">
          <span>Make: </span>{{item.make}}</br>
          <span>Model:</span> {{item.model}}</br>
          <span class="font-weight-bold">Weekly Rate:</span> ${{item.pointval}}
        </div>
        <a href="/product-detail/{{item.id}}" class="stretched-link"></a>
      </div>
    </div>


    {{!-- <div class="col-md-6">
      <div class="card flex-md-row mb-4 box-shadow h-md-250">
        <div class="card-body d-flex flex-column align-items-start">
          <img class="card-img-right flex-auto d-none d-md-block" alt="alt text" style="width: 200px; height: 200px;"
            src="{{item.img_url}}">

          <strong class="d-inline-block mb-2 text-primary">{{item.category_id}}</strong>
          <h3 class="mb-0">
            <a class="text-dark" href="http://localhost:3001/product-detail/{{item.id}}">{{item.name}}</a>
          </h3>
          <div class="mb-1 text-muted">Subtitle</div>
          <p class="card-text mb-auto">
            {{item.description}}
          </p>
          <a href="#">Call to Action</a>
        </div>

      </div>
    </div> --}}

    {{/each}}

  </div>
</div>